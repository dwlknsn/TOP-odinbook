<div id="<%= dom_id(profile) %>-full" class="flex flex-col gap card bg-main">
  <%= render partial: "profiles/preview", locals: { profile: profile } %>
  <%= render partial: "followings/followings", locals: { followings: followings } %>
  <%= render partial: "posts/preview", collection: profile.user.authored_posts, as: :post %>
</div>
