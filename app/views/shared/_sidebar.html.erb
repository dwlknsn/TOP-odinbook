<nav class="grow flex flex-col justify-between p-4">
  <ul class="flex flex-col gap">
    <% if user_signed_in? && current_user&.profile.persisted? %>
      <li>
        <%= link_to("My Feed", posts_path({ filter: :following })) %>
      </li>
      <li>
        <%= link_to("Discover", posts_path({ filter: :discover })) %>
      </li>
      <li>
        <%= link_to("Users", users_path) %>
      </li>
      <li>
        <%= link_to("Profile", profile_path) %>
      </li>
      <% if Rails.env.development? %>
        <li>
          <%= link_to("Developer Console", static_dev_console_path) %>
        </li>
      <% end %>
    <% end %>
    <% if !user_signed_in? %>
      <li>
        <%= link_to("Sign up", new_user_registration_path) %>
      </li>
      <li>
        <%= link_to("Log in", new_user_session_path) %>
      </li>
    <% end %>
  </ul>
  <ul class="flex flex-col gap">
    <% if user_signed_in? %>
      <li>
        <%= link_to("Log out", destroy_user_session_path, data: { turbo_method: :delete }) %>
      </li>
    <% end %>
  </ul>
</nav>
